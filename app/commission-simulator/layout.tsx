export const metadata = {
  title: "Funding Tier — Profit Engine",
  description: "Deal routing and revenue forecasting tool for Level Debt and Consumer Shield.",
};

export default function ProfitEngineLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
