export const metadata = {
  title: "Funding Tier — Commission Simulator",
  description: "Agent earnings calculator for Level Debt and Consumer Shield deals.",
};

export default function CommissionSimulatorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
